{
    "debug": true,

    "server": {
        "port": 3000
    },

    "cron": {
        "spec": "*/30 * * * * *"
    },

    "database": {
        "engine": "mysql",
        "host": "localhost",
        "name": "alanytics",
        "password": "password",
        "user": "alan"
    },

    "pid_dir": "/var/tmp/",

    "redis": {
        "host": "localhost",
        "port": 6379
    }
}

